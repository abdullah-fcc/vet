# ============================================
# FRONTEND (Vite exposes these to the browser)
# ============================================

# Backend API URL (your server that proxies Retell API)
VITE_API_URL=http://localhost:3000

# Retell Agent ID (from Retell dashboard → Agents → your agent)
VITE_RETELL_AGENT_ID=agent_xxxxxxxxxxxx

# ============================================
# BACKEND (server/.env — never exposed to browser)
# ============================================

# Your Retell API Key (Dashboard → API Keys)
RETELL_API_KEY=key_xxxxxxxxxxxx

# Same Agent ID for fallback on server side
RETELL_AGENT_ID=agent_xxxxxxxxxxxx

# Server port
PORT=3000

# CORS origin (your Vite dev server)
CORS_ORIGIN=http://localhost:5173
